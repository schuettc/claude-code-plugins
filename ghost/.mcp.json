{
  "mcpServers": {
    "ghost": {
      "command": "npx",
      "args": ["-y", "ghost-blog-mcp@latest"],
      "env": {
        "GHOST_API_URL": "${GHOST_API_URL}",
        "GHOST_ADMIN_API_KEY": "${GHOST_ADMIN_API_KEY}"
      }
    }
  }
}
